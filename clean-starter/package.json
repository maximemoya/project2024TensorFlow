{
  "name": "clean-starter",
  "version": "1.0.0",
  "description": "Clean starter kit with DDD, TDD, and Hexagonal Architecture",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd src/server && npm run dev",
    "dev:client": "cd src/client && npm run dev",
    "install:all": "npm install && cd src/server && npm install && cd ../client && npm install",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd src/server && npm test",
    "test:client": "cd src/client && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
